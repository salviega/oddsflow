# --- Node / JS / TS (pnpm) ---
node_modules/
.pnpm-store/
dist/
build/
*.tsbuildinfo
*.log
pnpm-debug.log*

# --- Next.js ---
.next/
next-env.d.ts

# --- Foundry ---
# broadcast/ is versioned as deployment evidence, except the local Anvil
# chain and dry runs.
packages/contracts/cache/
packages/contracts/out/
packages/contracts/broadcast/*/31337/
packages/contracts/broadcast/*/*/dry-run/

# --- Env and secrets ---
.env
.env.local
.env*.local
*.pem
*.key

# --- Deploy ---
.vercel

# --- Coverage ---
coverage/

# --- OS / editor ---
.DS_Store
Thumbs.db
.vscode/*
!.vscode/extensions.json
.idea/

# --- Claude Code local settings ---
.claude/settings.local.json

# Local fork deployments (scripts/dev-fork.sh)
packages/contracts/deployments/31337.json

# Next 16 writes these for AI agents; the repo root AGENTS.md is the only one
apps/web/AGENTS.md
apps/web/CLAUDE.md

# Vercel CLI (`vercel link`) adds this; keep the template tracked
.env*
!.env.example
